"use client";

export default function QuestionCard({ question, requirements, onChange, onDelete, onPin, onMove }) {
  const protectedQuestion = question.edited || question.pinned || question.source === "manual";
  return <article className={`rounded-2xl border bg-white p-5 transition-shadow hover:shadow-md ${protectedQuestion ? "border-[#92b9a1]" : "border-[var(--line)]"}`}><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-[var(--brand-soft)] px-2.5 py-1 text-xs font-black capitalize text-[var(--brand)]">{question.category.replaceAll("-", " ")}</span><span className="rounded-full bg-[var(--surface-muted)] px-2.5 py-1 text-xs font-bold text-[var(--muted)]">Difficulty {question.difficulty}/3</span>{protectedQuestion && <span className="rounded-full bg-[#fff1d9] px-2.5 py-1 text-xs font-bold text-[#8a5a00]">Protected</span>}</div><div className="flex gap-1"><button onClick={onMove(-1)} aria-label="Move question up" className="control-button">↑</button><button onClick={onMove(1)} aria-label="Move question down" className="control-button">↓</button><button onClick={onPin} className="control-button text-xs">{question.pinned ? "Unpin" : "Pin"}</button><button onClick={onDelete} className="control-button text-xs text-[var(--danger)]">Delete</button></div></div><label className="mt-5 block text-sm font-bold">Interview prompt<textarea value={question.prompt} onChange={(event) => onChange({ ...question, prompt: event.target.value, edited: true, source: question.source || "generated" })} className="field-input min-h-24 resize-y text-base font-bold leading-6" aria-label="Question prompt" /></label><label className="mt-4 block text-sm font-bold">Answer outline<textarea value={question.answer_outline} onChange={(event) => onChange({ ...question, answer_outline: event.target.value, edited: true, source: question.source || "generated" })} className="field-input min-h-24 resize-y font-normal leading-6" /></label><div className="mt-5 border-t border-[var(--line)] pt-4"><p className="text-xs font-black uppercase tracking-wider text-[var(--muted)]">Linked requirements</p><div className="mt-2 flex flex-wrap gap-2">{question.requirement_ids.map((id) => <span key={id} className="rounded-full bg-[var(--surface-muted)] px-2.5 py-1 text-xs font-semibold text-[var(--muted)]">{requirements.find((item) => item.id === id)?.text || id}</span>)}</div></div></article>;
}